-- Система домов кейса (код swisseph: P, K, R, C, O, E, W, B).
-- Пишется при каждом сохранении кейса (кроме синастрий): фиксирует, в какой
-- системе выносилось суждение — смена глобального дефолта не пересчитывает
-- сохранённые карты. NULL (до миграции / старые строки) = дефолт типа кейса.
-- Код толерантен к отсутствию колонки (insert-retry без extras, update с warn).

alter table charts add column if not exists house_system text;
