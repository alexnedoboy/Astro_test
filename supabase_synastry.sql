-- Миграция для синастрий (композитный тип кейса).
-- Выполнить в Supabase SQL Editor. Код приложения толерантен к отсутствию
-- колонки: до миграции синастрия работает в рамках сессии, автосохранение
-- молча падает (без крэша). Колонки case_type/folder уже добавлены раньше.
--
-- Синастрия — композитный кейс (WORKSPACE_CONCEPT §3/§9): своей сердцевины нет,
-- ядро строки пустое; refs хранит двух субъектов (ссылка chartId + snapshot полей).

alter table charts add column if not exists refs jsonb;   -- { a:{chartId,name,birth_date,…}, b:{…} }
